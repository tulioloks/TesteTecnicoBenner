<Window x:Class="TesteTecnicoBenner.Views.Pedidos.VisualizarPedidosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pedidos"
        Height="720" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Botões -->
            <RowDefinition Height="*"/>
            <!-- Grid de pedidos -->
            <RowDefinition Height="Auto"/>
            <!-- Grid de produtos -->
        </Grid.RowDefinitions>

        <!-- Botões -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Novo Pedido" Width="150" Margin="5" Click="NovoPedido_Click"/>
            <Button Content="Atualizar Lista" Width="150" Margin="5" Click="Atualizar_Click"/>
        </StackPanel>

        <!-- Grid de pedidos -->
        <GroupBox Header="Lista de Pedidos" Grid.Row="1" Margin="0,0,0,10">
            <DataGrid x:Name="dgPedidos"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      Height="300"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding NomePessoa}" Width="200"/>
                    <DataGridTextColumn Header="Data da Venda" Binding="{Binding DataVenda, StringFormat=d}" Width="120"/>
                    <DataGridTextColumn Header="Valor Total" Binding="{Binding ValorTotal, StringFormat=F2}" Width="100"/>
                    <DataGridTextColumn Header="Forma de Pagamento" Binding="{Binding FormaPagamento}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Grid de produtos do pedido -->
        <GroupBox Header="Produtos do Pedido Selecionado" Grid.Row="2">
            <DataGrid x:Name="dgProdutosDoPedido"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      Height="200"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Produto" Binding="{Binding Nome}" Width="200"/>
                    <DataGridTextColumn Header="Valor Unitário" Binding="{Binding ValorUnitario, StringFormat=F2}" Width="100"/>
                    <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="100"/>
                    <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=F2}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>
